import { PageHero } from "@/components/PageHero";
import { ContactForm } from "@/components/ContactForm";
import { site } from "@/lib/site";
export const metadata={title:"Contact"};
export default function Contact(){const socials=Object.entries(site.socials).filter(([,h])=>h);return <main><PageHero eyebrow="Contact" title="Tell Michael what you are navigating." lead="Reach out directly with the transition, decision or identity shift you want help thinking through."/><section className="section page-body"><div className="container form-shell"><div><div className="eyebrow">Direct contact</div><h2 className="display" style={{fontSize:"clamp(48px,5vw,82px)",margin:"18px 0 24px"}}>Start with the real situation.</h2><p className="body-lg">Name, email, phone, how you found GrowthGains and what you are trying to navigate. That is enough to begin.</p>{site.email&&<p className="body-md">Email: <a href={`mailto:${site.email}`}>{site.email}</a></p>}{socials.length>0&&<div className="footer-nav" style={{marginTop:25}}>{socials.map(([label,href])=><a key={label} href={href} target="_blank" rel="noreferrer">{label} ↗</a>)}</div>}</div><ContactForm/></div></section></main>}
